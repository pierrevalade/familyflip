= @device.name
= link_to "Edit", edit_device_path(@device)
<br />
<br />

= link_to "Add a new contact", new_contact_path(:device_id => @device.id)
<br />
<br />

%h1 All contacts for #{@device.name}

- @device.contacts.each do |contact|
  .contact
    .image= image_tag contact.image.url
    .name= link_to contact.name, contact_devices_path(:id => contact.id)
  <br /><br />