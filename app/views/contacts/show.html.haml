#page
  .content
    .contact
      .picture
        = image_tag(@contact.image_path, :width => 65)
      .name
        = @contact.name
    
    .phone
      = image_tag 'interface/bphone.png'
    .write
      = image_tag 'interface/bwrite.png'

    .clear

    - @messages.each do |message|
      = render message