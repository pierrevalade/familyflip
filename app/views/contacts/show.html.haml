#page
  .content
    = render @contact
    
    .phone
      = image_tag 'interface/bphone.png'
    .write
      = link_to image_tag('interface/bwrite.png'), new_message_path(:contact_id => @contact.id)

    .clear

    - @messages.each do |message|
      = render message
    
    #previous
      = link_to image_tag('previous.png'), paged_images_contacts_url(:id => @contact.id, :page => 2)
      
    #next
      = link_to image_tag('next.png'), paged_images_contacts_url(:id => @contact.id, :page => 2)