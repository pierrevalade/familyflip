.message
  .date
    = time_ago_in_words message.created_at
    ago
  .text
    = message.subject
    = simple_format message.text
  
  - if message.images?
    .image
      = image_tag message.image.url(:normal)